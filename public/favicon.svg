<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 500" style="background-color: #00000000; font-family: 'Arial', sans-serif;">
    <!-- Define colors -->
    <defs>
        <style>
            .dark-color { fill: #1A2333; }
            .orange-color { stroke: #E86D20; fill: none; }
            .light-color { stroke: white; fill: none; }
        </style>
    </defs>

    <!-- Logo Group -->
    <g transform="translate(0, )">

        <!-- 1. Document Icon Body (Dark Color) -->
        <rect class="dark-color" x="50" y="80" width="120" height="140" rx="15" ry="15"/>

        <!-- 2. List Items (White Lines) -->
        <g class="light-color" stroke-width="8" stroke-linecap="round">
            <!-- Short line (top right) -->
            <line x1="120" y1="105" x2="160" y2="105" />
            <!-- Middle line -->
            <line x1="70" y1="145" x2="150" y2="145" />
            <!-- Bottom line -->
            <line x1="70" y1="185" x2="150" y2="185" />
        </g>

        <!-- 3. Checkmark (Orange V) -->
        <polyline class="orange-color"
                  points="80 110, 95 125, 115 100"
                  stroke-width="10"
                  stroke-linejoin="round"
                  stroke-linecap="round" />
    </g>
</svg>