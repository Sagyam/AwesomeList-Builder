# GitHub Personal Access Token
# Generate one at: https://github.com/settings/tokens
# Required scopes: public_repo (or repo for private repos)
# This increases your rate limit from 60 to 5000 requests per hour
# Note: Use GH_API_TOKEN (not GITHUB_TOKEN) to avoid conflicts with GitHub Actions
GH_API_TOKEN=your_github_token_here

# GitLab Personal Access Token
# Generate one at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: read_api
# This increases your rate limit from 300 to 5000 requests per hour
GITLAB_TOKEN=your_gitlab_token_here

# Youtube token
YOUTUBE_TOKEN=your_youtube_token_here

# Build Configuration
# Enable/disable data fetching during build (default: true)
ENABLE_DATA_FETCHING=true

# Screenshot Feature
# Enable/disable automatic screenshot capture for missing og:images (default: true)
ENABLE_SCREENSHOTS=true

# Granular Cache Configuration
# Configure cache TTLs in src/data/metadata.yaml:
# - metadata.ttl: How often to refresh GitHub stars, npm downloads, etc. (in days)
# - screenshots.regenerate: Set to false to skip regenerating existing screenshots
# - ai.ttl: How long to cache AI-generated metadata (in days, for future use)
